server {
  listen       80;
  server_name  localhost;

  location /static/ {
    root   /opt/isucon/public;
    index  index.html index.htm;
  }

  location / {
    proxy_pass   http://webapp:8000;
  }
}
