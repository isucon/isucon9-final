server {
  listen       80;
  server_name  localhost;

  location / {
    root   /opt/isucon/public;
    index  index.html index.htm;
  }

  location /initialize {
    proxy_pass   http://webapp:8000;
  }

  location /api {
    proxy_pass   http://webapp:8000;
  }
}
